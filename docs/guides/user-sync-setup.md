# User Data Synchronization Setup

- Automatic user sync on sign up/login (Clerk webhooks)
- Customer linking on first purchase (Paddle webhook)
- Proper Clerk ↔ Paddle mapping

Webhook URLs:

- `/api/clerk-webhook`
- `/api/paddle/webhook`
